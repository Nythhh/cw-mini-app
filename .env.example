# Bot Telegram du shop (mini-app client) — validation initData
TELEGRAM_BOT_TOKEN=

# Admin — mot de passe du panneau /admin (min. 6 caractères en prod)
ADMIN_PASSWORD=

# Secret pour signer les cookies de session admin (chaîne longue aléatoire)
ADMIN_SESSION_SECRET=

# Upstash Redis (prod Vercel) — persistance catalogue / stock
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
